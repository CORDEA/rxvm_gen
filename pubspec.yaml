name: rxvm_gen
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  build: ^2.0.2
  source_gen: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.0.4

flutter:
